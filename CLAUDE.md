# Claude Code Instructions

## Git Workflow

**Always push directly to `main`.** Do not create feature branches, do not use intermediate branches. All commits go straight to `main`:

```
git push -u origin main
```
